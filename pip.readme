The pip file is a simple whitespace (newline) delimited list of
Python packages needed for core to run. To install all the
packages at once, simple run a command like this:

    sudo apt install `cat pip`

Related files:

    deb
    deb.readme
