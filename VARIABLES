accompany
    Used as a boolean to indicate that a getter should include `self`
    along with the collection of objects that it is returning. See:
    dom.element.getsiblings

ex 
    Exception. Typically found in exception blocks.

        try:
            ...
        except AttributeError as ex:
            ...
        except Exception as ex:
            ...
v 
    Value. Used in @property setters as the argument that contains the
    value the property should be set to.

x
    The default varibale in list comprehensions:
        
        ls1 =[x for x in ls]
